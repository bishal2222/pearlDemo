{
  "scripts":{
    "open-cypress":"cypress open",
    "open-cypress:chrome": "cypress run --headed --browser chrome",
    "open-cypress:firefox": "cypress run --headed --browser firefox"
  },

  "devDependencies": {
    "cypress": "^13.10.0"
  }
}
